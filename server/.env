PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/chat-app
JWT_SECRET=replace_this_with_a_long_random_secret_string
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# Google Sign-In (see README "Google Sign-In Setup" section)
# The Client ID is public and safe to commit to this example file.
GOOGLE_CLIENT_ID=1093330188611-qnnfido5bn4lbths8ka5b9k09nrae50t.apps.googleusercontent.com
# NEVER put your Client Secret in a file that gets shared or committed.
# The backend does not actually need the secret for ID-token verification,
# so it is intentionally left out here.
